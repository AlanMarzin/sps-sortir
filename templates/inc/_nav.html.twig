<nav>
    <ul>
            {% if app.user %}
                <li>
                    <a href="{{ path('profil_update', {"id": app.user.id}) }}">Modifier mon compte</a>
                    <a href="{{ path('user_details', {"id": app.user.id}) }}">Afficher détails utilisateur</a>
                </li>
                <li>
                    <a href="{{ path('app_logout') }}">Deconnexion</a>
                </li>
            {% endif %}
    </ul>
</nav>